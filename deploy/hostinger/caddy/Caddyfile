# Edit the hostnames below to match the DNS records pointed at this VPS.
# Caddy auto-provisions TLS certs from Let's Encrypt on first request.
#
# Reload after edits:  sudo systemctl reload caddy

{
    # Set an email for ACME registration (Let's Encrypt expiry alerts go here).
    email ops@fracktal.in
    admin off
}

# --- Gateway API (FastAPI) ---
api.metorite.fracktal.in {
    encode zstd gzip
    reverse_proxy 127.0.0.1:8080 {
        header_up X-Real-IP {remote_host}
    }
}

# --- Control Plane UI (Next.js) ---
metorite.fracktal.in {
    encode zstd gzip
    reverse_proxy 127.0.0.1:3001
}