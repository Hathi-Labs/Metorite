# Core Module Map — Orchestration / Agent / Chat Systems

> **Status:** Living review checklist · **Created:** 2026-07-02
> Atomic inventory of the platform core (apps like email/task-manager/drawio are OUT of scope here).
> Each module maps to its [awesome-harness-engineering](https://github.com/ai-boost/awesome-harness-engineering) practice area so module-by-module completeness reviews compare against current best practice.
> Companions: [`harness_hardening_2026-07.md`](harness_hardening_2026-07.md) (HH queue) · [`archive/chat_implementation_review_2026-07.md`](archive/chat_implementation_review_2026-07.md) (chat audit) · [`llm_caching_memory.md`](llm_caching_memory.md) · [`observability_e2.md`](observability_e2.md) (**debugging runbook** — how to trace "error X with agent Y") · [`permissions_sandbox_b6.md`](permissions_sandbox_b6.md) (safety) · [`archive/context_assembly_c2.md`](archive/context_assembly_c2.md).
> Grades: **A** solid, best-practice-aligned · **B** works, known gaps · **C** significant gaps / drift risk · **D** scaffold or missing.

## A. Agent Execution Core (orchestrator)

| # | Module | Lives in | List section | Grade | Key gaps |
|---|---|---|---|---|---|
| A1 | **Agent loop / executor** — run_agent_stream/run_agent, 3-tier dispatch (native MAF stream / Copilot SDK / batch shim), watchdogs, retries | `apps/orchestrator/orchestrator/executor.py` (~4.2k lines) + `watchdog.py` | Agent Loop | **B+** | Monolith remains (main open item). **2026-07-02**: P1-2 cross-worker control bus; **watchdog policy unified** into `watchdog.py` (`WatchdogPolicy` values + native tier-selection, one source of truth — was 3 inline env reads); **loop detection** added (native path trips RUN_ERROR when a tool repeats with identical args, `LoopDetector`); **P1-6 Copilot session continuity already implemented** (in-mem + Postgres-durable `service_session_id` store, restore+retry, model-switch detection) — prior "no session continuity" note was stale; P1-5 emit-ordinal cursor plumbing in place (skip-by-count deferred — needs client delta-dedup + UI drive) |
| A2 | **Event translation** — `orchestrator/event_translator.py` (ONE canonical mapping + TranslatorHooks), per-message dedup | `event_translator.py`; folds now segment-native + id-less fallback in `chat/route.ts` + `chatStream.ts` + `chat_fold.py` | Agent Loop / AG-UI | **A** | Unified 2026-07-02 (Phase 2, parity evals). Phase 3b made rendering segment-native (client prefers real segments; fold demoted to id-less fallback). **Phase 3c (2026-07-03, 12eee23):** EVERY assistant text segment is answer BODY (VS Code parity) — pre-tool text no longer buried in the ThinkingContainer; fixed the same way across all 4 parity layers. Watchdog policy unified into `watchdog.py`. No open items — one canonical translator + one watchdog policy, both eval-locked |
| A3 | **Stream relay** — Redis Streams per thread, detached runs, replay/subscribe cursors, cancel cascade, cross-worker control bus | `apps/orchestrator/orchestrator/stream_relay.py` | Long-running agents / observability | **A** | Invariants locked by trajectory evals. P0-3 persistence owner shipped (`on_complete` hook); P1-2 cross-worker cancel/HITL via Redis pub/sub; P1-5 emit-ordinal cursor plumbing in place. Only the reconnect skip-by-count optimization remains (deferred — needs client delta-dedup + UI drive) |
| A4 | **Sub-agent orchestration** — call_agent family, SUB_AGENT_* event forwarding, depth/cycle guards, tier inheritance, background children | `packages/acb_skills/acb_skills/agent_tools.py`, `executor._run_sub_agent_streaming` | Multi-agent orchestration | **B** | Handoff = message string only (no typed schema/history/memory) — HH-7; child injected-tool events un-namespaced in parent |
| A5 | **HITL subsystem** — ask_user/ask_questions/request_confirmation blocking futures, respond-input, HITL-aware watchdog, mutation inbox | `acb_skills/ask_tools.py`, `executor._pending_user_input`, gateway `/respond-input` | Human-in-the-Loop | **A−** | Fail-closed shipped (HH-2). **P1-2 closed 2026-07-02**: a HITL answer that lands on a different worker is now relayed over the control bus to the worker holding the Future (respond-input carries thread_id). Remaining: 3 delivery paths (A/B/C) is complexity debt |

## B. Agent System (lifecycle & capability)

| # | Module | Lives in | List section | Grade | Key gaps |
|---|---|---|---|---|---|
| B1 | **Dynamic agent loader** — clone/pull, importlib `build_agents()`, static+DB registry, config.json contract, push-guard hook, auto-dep install | `packages/acb_skills/acb_skills/loader.py`, `gateway/routes/agent.py` `_AGENT_REGISTRY` | Governing agents | **B+** | In-process import = no isolation (→ B6); `runtime` field authoritative but drift between registry sources possible |
| B2 | **Tool injection & scoping** — `_inject_agent_tools` (4 agent shapes), `tool_scope` + `own_tool_scope`, addendum builder, `normalize_tools` | `executor.py` 546–760 | Tool Design | **B+** | own_tool_scope shipped (HH-5) but email subset unset; addendum is hand-maintained prose (drift risk vs actual tools) |
| B3 | **Platform tool suite** — web, artifacts, memory, todos, notes, history, github search, errors, deps + risk annotations | `packages/acb_skills/*_tools.py`, `tool_annotations.py` | Tool Design / Skills | **B+** | Annotations shipped (HH-2); no per-tool eval coverage; error messages not systematically agent-optimised ("Writing Effective Tools") |
| B4 | **MCP integration** — `mcp_servers` table, per-run injection, agent_scope filter | `executor._inject_mcp_servers`, spec `mcp_plugin_integration.md` | Skills & MCP | **B** | No tool budgeting/identity propagation (list: "MCP production gaps"); no MCP Inspector-style debug path |
| B5 | **Integration registry** — encrypted Postgres creds, OAuth flows, env/`mcp_servers=` injection | `acb_skills/integrations.py`, gateway `/integrations` | Permissions & Authorization | **A−** | On-behalf-of vs fixed-credential distinction implicit; audit granularity per-operator unresolved (plan open Q7) |
| B6 | **Permissions & sandboxing** — risk-aware permission handler (blocks dangerous shell + out-of-workspace writes, defers destructive tools to their confirm gate, logs every privileged op), in-process agent code, workspace visibility rules | `packages/acb_skills/acb_skills/permission_policy.py`, `executor.py` (`_copilot_permission_handler` + 5 handler sites + `_make_tool_shim`), `tool_annotations.py` | Security, Sandbox & Permissions | **B−** | **2026-07-03 (HH-6 near-term):** `PermissionHandler.approve_all` replaced by a risk-aware handler at all 5 Copilot sites — reads the SDK's own `PermissionRequest` classification (shell command text / file paths / URLs / read-only) + our risk annotations; dangerous shell (rm -rf /, fork bomb, curl\|sh) and out-of-workspace writes fail CLOSED, destructive tools defer to `request_confirmation` (no double-gate), unknowns fail open-but-logged; `AGENT_PERMISSION_MODE` enforce\|audit\|approve_all. Remaining: native-MAF **Tier-1 streaming** has no tool choke point (needs callable-wrapping at injection); **container isolation for normal runs** (in-process `importlib` model unchanged) is the Phase-5 item + the real residual excessive-agency exposure |
| B7 | **Self-mutation** — mutation container, 1-attempt cap, commit gate + auto-push on green, HITL approval, rollback | `orchestrator/mutation.py`, `Dockerfile.mutation`, `mutation_runner.py` | Verification & CI | **A−** | PR automation (WBS 1.3) + mutation audit surfacing (1.5) still open; eval gate inside sandbox is pytest-only |

## C. Model & Context Layer

| # | Module | Lives in | List section | Grade | Key gaps |
|---|---|---|---|---|---|
| C1 | **LLM gateway & tier routing** — LiteLLM SDK, 3 tiers + aliases, encrypted key store, model_config DB, BYOK, v1_compat | `packages/acb_llm/client.py`, `key_store.py`, `infra/litellm/` | LiteLLM / routing | **A−** | Retry telemetry now logged (HH-3); JSON-mode audit across call sites incomplete (known deepseek empty-content issue); no complexity/cost-aware routing |
| C2 | **Context assembly** — ONE server-side run-context assembler (`acb_llm.assemble_run_context`): token-budgeted windowing + current-turn dedup + DB-rebuild-when-empty; session-scoped memory enrichment | `acb_llm/context.py`, `executor.py` (both structured-message sites), `agent.py` (DB loader), `chat/route.ts` (UI ring) | Context Delivery & Compaction | **A−** | **2026-07-03**: unified the six divergent client-history slicers (caps 12/16/20/50, char-truncation, token-blind) into one token-budgeted server assembler shared by the streaming + batch paths (parity, no drift); non-chat callers (API/webhook) now rebuild history from the `chat_message` store via an injected loader → context parity; memory enrichment session-scoped (C4). Remaining: the client still assembles for the browser UI (additive, not trusted); email path has its own string-flatten assembler (app-scoped) |
| C3 | **Compaction** — manual + auto (80% of real model window, hysteresis, checkpoint model, active-window slicing) | `AgentChat.tsx` 475–592, `compact/route.ts`, `lib/tokenCount.ts` | Compaction | **A−** | Frontend-only (API/webhook runs never compact); summariser model hardcoded to one provider |
| C4 | **Prompt caching** — provider-aware transform (Anthropic explicit `cache_control` on the stable system block + tool array; OpenAI `prompt_cache_key`; sentinel) + session-scoped memory + LiteLLM Redis exact-match cache + startup prewarm | `packages/acb_llm/acb_llm/prompt_cache.py`, `client.py`, `v1_compat.py`, `executor.py`, `agents.py`, `session_cache.py`, `main.py` | Prompt Caching | **A−** | **2026-07-03 — shipped, ADR-008 implemented.** Single `apply_prompt_caching` transform at BOTH choke points (SDK-direct, no proxy — the plan's proxy-hook design was corrected to a shared transform); sentinel-marked stable/dynamic boundary; tool-array caching for function-tool agents; session-scoped memory; LiteLLM Redis cache (opt-in); Anthropic startup prewarm. 23 caching tests (5 CI-blocking trajectory). Remaining: activates fully only when a tier resolves to Anthropic/OpenAI (default DeepSeek uses automatic caching); Phase 7 email tool-surface reduction is app-scoped |
| C5 | **Memory subsystem** — Mem0 (pgvector) episodic, Graphiti bi-temporal KG, active tools, NOTES.md pattern, run-start enrichment, run-boundary extraction | `packages/acb_memory/`, `acb_skills/memory_tools.py`, `note_tools.py` | Memory & State | **A−** | **P1-9 closed 2026-07-02**: extraction now fires at the gateway run boundary for both orchestrator paths (survives reconnect/browser-gone, includes the folded answer). Remaining: no write-gate on Graphiti (every turn writes); email↔agent memory bridge pending |

## D. Chat System (frontend + API)

| # | Module | Lives in | List section | Grade | Key gaps |
|---|---|---|---|---|---|
| D1 | **Chat API layer** — SSE proxy/translator (live path), respond-input proxy, Mem0 extraction hook; authoritative persistence now gateway-side (`chat_fold.py`) | `workbench/.../api/agent/chat/route.ts`, `api/chat/sessions/*`, `gateway/chat_fold.py` | Agent Loop (context assembly resp.) | **B+** | P0-3 closed both paths (Phase 1/2); Phase 3b made the client segment-native. **P1-9 closed 2026-07-02**: both orchestrator paths extract memory at the gateway run boundary (fires on reconnect/cancel, includes the folded answer) via one shared assembler `build_extraction_conversation`; route.ts no longer extracts (dead code removed). **Phase 3c (2026-07-03):** `chat_fold.py` server `content` = all non-empty segments joined (was last-segment-biased), keeping route.ts + chat_fold.py identical (see A2/D2). Remaining: route still owns history assembly + a live fold that's now demotable but path-conditioned + needs a UI-drive (see spec); P1-5 reconnect placeholder ids; `lastEventId`≈0-0 full replays |
| D2 | **Stream reducer & store** — `applyStreamEvent` fold helpers, ownership tokens, `_stream_id` dedup, StrictMode guards | `src/lib/chatStream.ts`, `chatStore.ts`, `hooks/useAgentChat.ts` | AG-UI | **B+** | Centralised reducer shipped; duplicate-token guards must not be removed; 2 refactors deliberately skipped (documented). **Phase 3c (2026-07-03):** `tool_start` no longer folds pre-tool assistant text into `reasoningBlocks` — segments are the body source of truth (id-less legacy runtimes keep the old fold); `MarkdownMessage.tsx` renders ALL segments as body |
| D3 | **Chat UI surfaces** — AgentChat, tool/HITL/artifact/todo cards, context ring, mobile drawer, sub-agent timeline | `src/components/AgentChat.tsx` + cards | A2UI / AG-UI | **B+** | AgentChat is its own monolith (~2k lines); card lifecycle hardening done batches 1–3 |
| D4 | **Session & model management** — sessions CRUD, model picker + tier lock, per-agent model resolution chain | `api/chat/sessions`, `_apply_model_for_maf_agent`, `_active_run_model` | — | **B+** | Copilot `service_session_id` store is Postgres-durable (cross-worker + cross-restart) with restore+retry (P1-6); cross-worker run control via the P1-2 bus. Remaining: model-resolution chain spans 3 layers (documented in memory, not one code location) |

## E. Cross-cutting

| # | Module | Lives in | List section | Grade | Key gaps |
|---|---|---|---|---|---|
| E1 | **Evals & CI gates** — trajectory evals (blocking), inspect smoke, promptfoo golden cases, pr-check, deploy gates | `evals/`, `.github/workflows/` | Evals & Verification | **B** | Was D before HH-1; promptfoo gate inactive until CI secrets; coverage = harness invariants only, no agent-quality evals yet |
| E2 | **Observability** — correlated JSON logs (run_id/agent/user via contextvars), durable per-run trace store (`agent_run`), `/debug/runs` diagnostics API, one-command VPS feature-check harness, audit_event, run-attributed usage telemetry, OTEL callback (gated) | `acb_common` logging (`bind_run_context`), `apps/gateway/gateway/run_trace.py` + `routes/debug.py` + `infra/postgres/50_agent_run_trace.sql`, `scripts/feature_check.py`, `tests/integration/test_chat_features.py` | Observability & Tracing | **A−** | **2026-07-03 (Phases 1–4):** every log line for a run is filterable (contextvar-bound) + JSON-able (`LOG_FORMAT=json`); durable `agent_run` row per run (metadata all runs, full trace + traceback for errored/flagged) survives the 1h Redis TTL; `GET /debug/runs` + `/debug/runs/{id}` query it (EXECUTIVE-gated, no SSH needed); `scripts/feature_check.py` sweeps chat + each AI app live and reports pass/fail + the run_id to debug. The debug loop is self-serve. Remaining: OTLP trace-backend export dormant (gated on `OTEL_EXPORTER_OTLP_ENDPOINT`) — the only E2 open item |

## Review order (impact-first, core-only)

Impact = (how much every run touches it) × (distance from best practice). Apps excluded per scope.

1. ~~**A1+A2+D1 — Executor / event translation / chat API**~~ **DONE (2026-07-02)** — 4-way duplication collapsed to one translator; P0-3/P1-2/P1-9 closed; segment-native rendering; unified watchdog + loop detection. Grades A1 B+, A2 A, D1 B+. Residual (all lower-impact / gated): executor monolith split, route history-assembly + live-fold demotion (UI-drive-gated), reconnect skip-by-count.
2. ~~**C2+C4 — Context assembly + prompt caching**~~ **DONE (2026-07-03)** — C4 prompt-caching core shipped (provider-aware transform at both SDK choke points; sentinel boundary; tool-array caching; session-scoped memory; LiteLLM Redis cache; startup prewarm) → **A−**. C2 server-side context assembly shipped (one `acb_llm.assemble_run_context`: token-budgeted windowing replaces six divergent count caps; streaming/batch parity; DB-rebuild for non-chat callers) → **A−**. Residual: C4 fully activates only on Anthropic/OpenAI tiers; Phase 7 email tool-surface reduction is app-scoped; C2 client still assembles for the UI ring (additive). **← next: B6 sandboxing.**
3. ~~**B6 — Permissions & sandboxing**~~ **near-term DONE (2026-07-03)** — risk-aware permission handler replaced `approve_all` (dangerous shell + out-of-workspace writes fail closed; every privileged op logged). C → **B−**. Residual: native-MAF Tier-1 streaming choke point + container isolation for normal runs (Phase 5) — the deferred deep-isolation work. **← next: A4 typed sub-agent handoffs, or the Phase-5 container isolation for a full B6 close.**
4. **A4 — Sub-agent orchestration** — typed handoffs (HH-7) after the translation refactor stabilises the boundary.
5. **A5 — HITL** — collapse 3 delivery paths to 1 (multi-worker-safe pending state now DONE via the P1-2 control bus).
6. **B2+B3 — Tool injection + platform tools** — generate the addendum from tool metadata (kill prose drift); per-tool eval cases.
7. **C5 — Memory** — Graphiti write-gate, extraction-path parity.
8. **C1 — LLM gateway** — JSON-mode audit, cost-aware routing pass.
9. **B4 — MCP** — tool budgeting, debug tooling.
10. **B1/B5/B7, A3, D2–D4, E1–E2** — already A−/B+ or scheduled elsewhere; sweep last.

## State-of-the-art comparison vs [awesome-harness-engineering](https://github.com/ai-boost/awesome-harness-engineering) (2026-07-02)

Reviewed the A1+A2+D1 work (and adjacent modules) against the list's named
practices. Verdict per area — **=SOTA** (matches/exceeds the documented best
practice), **~solid** (correct but with a known gap), **gap** (below best
practice):

| List practice (exact wording) | Our implementation | Verdict |
|---|---|---|
| **"hibernate-and-wake" persistence** · "Checkpointing: Resumption across context windows" (Agent Loop) | Redis-Stream event log per thread is the source of truth; `run_detached` decouples execution from the HTTP response; reconnect replays from the log + follows live. A run survives browser close AND (P1-2) cross-worker control. | **=SOTA** — this is the "decouple the brain from the hands" pattern implemented end-to-end, eval-locked. |
| **"Real-time streaming: AG-UI protocol for agent-to-UI communication with state updates"** (HITL) · **"Event-driven: Multi-agent streaming with shared event bus"** (Orchestration) | One canonical AG-UI translator (`event_translator.py`) feeds all four executor paths; Phase 3b makes it message-id-native (real segment boundaries, not inferred). Redis Stream = the shared event bus; SUB_AGENT_* forwarded. | **=SOTA** for the protocol + single-translator discipline; the list treats AG-UI as the reference and we implement it with parity evals. |
| **"HITL interrupts: Pause agent execution for human review"** · **"Approval flows: Structured permissions before destructive actions"** (HITL) | Blocking-Future HITL (ask_user/ask_questions/request_confirmation); confirmations **fail closed** for destructive/irreversible actions (HH-2); P1-2 makes the answer reach the owning worker. | **=SOTA** on fail-closed + real interrupt-and-resume (many harnesses only queue a reply); ~gap: 3 delivery paths is internal complexity debt. |
| **"Beyond Permission Prompts—structured authorization"** · tool annotations `readOnlyHint`/`destructiveHint`/`idempotentHint`/`openWorldHint` (Permissions) | `tool_annotations.py` risk vocabulary + `own_tool_scope`; confirmations gate on `is_destructive`; **2026-07-03: a risk-aware permission handler now gates every Copilot tool/shell/file/network request** on the SDK's own classification + the annotations (dangerous shell + out-of-workspace writes fail closed; destructive tools defer to their confirm gate; all privileged ops logged) — `approve_all` is gone (B6 C→B−). | **~solid → good** — structured authorization + fail-closed on the dangerous cases + full audit trail shipped; **remaining gap**: normal runs are still in-process (no container isolation — Phase 5), which is the real residual excessive-agency exposure. |
| **"Track all agent decisions for regression detection"** · **"Eval harnesses: measure agent behavior, not just outputs"** (Verification) | Trajectory evals are CI-blocking and cover the harness invariants (translator parity, fold, persistence, cross-worker control, watchdog, loop detection). | **~solid** — the *harness* is eval-locked (rare + strong); **gap**: no agent-*quality* evals yet (behavior/outcome scoring), which is the list's emphasis. |
| **Loop / failure detection** (implied by "verify" stage + regression tracking) | `LoopDetector` trips a RUN_ERROR on identical repeated tool calls; unified `WatchdogPolicy` bounds idle/tool/HITL stalls. | **=SOTA** — explicit loop + stall guards with one inspectable policy; most harnesses rely on a bare wall-clock timeout. |
| **"Cross-session: Persistent planning documents"** · **"LLM-curated memory with learned relevance filtering"** (Memory) | Mem0 (pgvector) episodic + Graphiti bi-temporal KG + NOTES.md; P1-9 extracts at the run boundary (survives reconnect, includes the answer). | **~solid** — dual-store memory + run-boundary extraction is ahead of typical; **gap**: no write-gate on Graphiti (writes every turn), no learned relevance filter. |
| **"Context as constraint" · "Compaction (84% token reduction)"** (Context) | Auto-compaction at 80% of the real window w/ hysteresis + checkpoint model. | **~solid**; **gap**: frontend-only (API/webhook runs never compact); history assembly still client-side (C2=C+). |
| **Prompt Caching** (byte-stable prefixes) | Provider-aware `apply_prompt_caching` transform at BOTH SDK choke points: Anthropic explicit `cache_control` on the stable system block (split at a sentinel) + the tool array; OpenAI `prompt_cache_key` routing; session-scoped memory (byte-stable across turns); LiteLLM Redis exact-match cache; Anthropic startup prewarm. Measurable via HH-3 counters; 5 CI-blocking trajectory evals. | **=SOTA** on mechanism — explicit breakpoints + tools-first caching + session-stable memory + exact-match layer is the full documented playbook. **~solid** in effect: fully activates only when a tier resolves to Anthropic/OpenAI (default DeepSeek does automatic context caching); tool-surface reduction (Phase 7) still pending for the email agent. C4 D → **A−**. |
| **Multi-worker / distributed correctness** (Governing agents; implied by managed-agent scaling) | P1-2 cross-worker control bus (Redis pub/sub) + Postgres-durable session store → cancel/HITL/session survive across workers. | **=SOTA** — the list gestures at "decouple brain from hands"; we implement the distributed-systems corollary (control commands route to the owning worker) explicitly. |

**Bottom line:** on the **agent loop, streaming/event protocol, HITL, long-running + multi-worker correctness, loop/stall detection, and now prompt caching** — the A1/A2/A3/A5 core plus C4 — Metorite is **at or above the documented state of the art**, and unusually, the harness itself is locked by CI-blocking trajectory evals (a practice the list preaches but few implement). As of 2026-07-03, all four flagged core gaps have had their near-term slice shipped: **C4** prompt caching (A−), **C2** context assembly (A−), **E2** observability (A−), and **B6** permissions (C → B−: `approve_all` replaced by a risk-aware handler that fails closed on dangerous shell + out-of-workspace writes and logs every privileged op). The remaining core work is now **depth, not gaps**: B6's *container isolation for normal runs* (Phase 5 — the in-process execution model is the real residual excessive-agency exposure), agent-*quality* evals (E1=B), and A4 typed sub-agent handoffs. None are correctness bugs.

## Status log
- 2026-07-04 — **B6 Phase-5 Tier 0 (per-run credential scoping) shipped — closes the top standing isolation exposure.** B6 stays **B−** (full container isolation for normal runs is still Tier 2), but the single worst *concrete* hole is closed. Recon for the deferred Phase-5 container work surfaced that `executor._inject_integrations_to_env` wrote every resolved integration secret (`ZOHO_REFRESH_TOKEN`, `CLICKUP_API_TOKEN`, `SMTP_PASSWORD`, `APIFY_API_TOKEN`, Gmail/Sheets SA paths, …) into the shared gateway `os.environ` and **never cleared it** (`if val and not os.environ.get(env_var)` — first-writer-wins, no teardown) at all three run sites (sub-agent/streaming/batch). Effect: every secret ever used accumulated in the long-lived process env, so any agent — including a prompt-injected one — could `os.getenv` another integration's secret regardless of its own `config.json` scope. Fix (executor.py): `_inject_integrations_to_env` now returns a **restore token** (prior value of each var it SET; operator-provided vars are neither overwritten nor recorded, so `.env` still wins) and `_restore_integration_env(token)` tears it down at each run's teardown boundary (batch `AsyncExitStack` callback, streaming + sub-agent `finally`) — creds are scoped to the run that declared them and gone before the next/idle agent runs. Honest limit recorded: `os.environ` is process-global so concurrent overlapping in-process runs still share the env for the overlap window — a real per-run env is Tier 2 (container/subprocess); Tier 0 removes the *permanent accumulation*, the steady-state exposure. Full tiered Phase-5 plan (Tier 0 creds → Tier 1 mutation-container resource/egress limits → Tier 2 generalized live streaming sandbox + tool-proxy RPC + per-agent venv on a warm pool for the 4GB VPS → Tier 3 default-deny) authored in [`permissions_sandbox_b6.md`](permissions_sandbox_b6.md). 9 unit + 2 CI-blocking trajectory evals (the cross-run no-accumulation security invariant is the contract); full suite 683 unit + 88 trajectory green, zero regressions. See [`permissions_sandbox_b6.md`](permissions_sandbox_b6.md) § "B6 Phase 5".
- 2026-07-03 — **Phase 3c (chat segment→body rendering) shipped — user-reported render bug.** No grade change (A2 stays **A**, D1/D2 stay **B+** — this closes a correctness bug, not a structural gap). Reported on agent-startup-guru: substantive answer text a model emitted *right before* a tool call ("Yes, I DO have access to memory_db.py… Let me fix this now. I'll:…") was wrapped into the collapsed ThinkingContainer. Root cause: the runtime emits distinct message-id segments (text A → tool → text B) and the renderer used a BINARY split — LAST segment = body, ALL earlier = narration → ThinkingContainer — burying the pre-tool answer. Fix (VS Code parity: every assistant `TEXT_MESSAGE_CONTENT` segment is answer body, tool cards interleaved; only genuine chain-of-thought + tools go in the ThinkingContainer) applied the same way across all 4 parity layers: `MarkdownMessage.tsx` (`answerBody` = all non-empty segments joined, `narrationSegments = []` — THE render fix), `lib/chatStream.ts` `tool_start` (stop folding pre-tool text into `reasoningBlocks`), `route.ts` + `chat_fold.py` (persisted/server `content` = all segments joined; blank any segment text stranded in reasoning so it isn't shown twice). Diagnosed by driving a real narrate→tool→answer run through `/agent/run/stream` and reading the raw SSE + the persisted `chat_message` (via the E2 run trace — the observability work paying off). Verified live on the VPS (persisted `content` holds BOTH segments, `reasoning_head` empty) AND in a real browser via Playwright ("VS Code-style: ALL assistant text is body" e2e). Tests: `evals/trajectories/test_chat_fold_trajectory.py::test_segment_native_all_text_is_body_vscode_style` + the e2e; full suite 708 Python + trajectory evals green, tsc clean. Shipped 12eee23. See memory `chat-segment-body-rendering.md`.
- 2026-07-02 — Map created from the harness-engineering audit + chat implementation review.
- 2026-07-02 — Review #1 (A1+A2+D1) design fleshed out in [`archive/core_loop_unification.md`](archive/core_loop_unification.md); Phase 1 (one persistence owner, P0-3) shipped for the named-agent path. D1 grade C+ → B− (persistence no longer client-only).
- 2026-07-02 — Phase 2 shipped (one event translator; P0-3 closed on `/copilot/chat` too). Grades: A1 B− → **B**, A2 C+ → **B+**, D1 stays **B−**. Review #1 remaining: Phase 3 (message-id-native rendering — deletes the three parallel folds), unified watchdog policy, P1-2/P1-5/P1-6/P1-9, `lastEventId` cursoring, §6 doc drift.
- 2026-07-02 — Phase 3b shipped (segment-native rendering, driven against the real UI; fold demoted to id-less fallback). Grades: A2 B+ → **A−**, D1 B− → **B**. Remaining Review #1: P1-2/P1-5/P1-6/P1-9, unified watchdog, `lastEventId` cursoring, route history-assembly + demotable live fold, §6 doc drift.
- 2026-07-02 — **P1-2 shipped** (cross-worker control bus). Grades: A1 stays **B** (still a monolith; watchdog/P1-6/loop-detection open), A5 B+ → **A−**. A thin Redis pub/sub relay in `stream_relay.py` (`register_control_command`/`dispatch_control`/`publish_control` + a per-run `_control_listener`): `cancel_run` and `/respond-input` try the local task/Future first, then relay to the owning worker on a miss (respond-input now carries `thread_id`). Locked by 4 cross-worker trajectory evals on a pub/sub FakeRedis; also fixed pre-existing integration-test loop-binding flakiness. Review #1 remaining: P1-5/P1-6/P1-9, unified watchdog, `lastEventId` cursoring, route history-assembly + demotable live fold, §6 doc drift.
- 2026-07-02 — **P1-9 shipped** (memory extraction at the gateway run boundary, both orchestrator paths). Grade: D1 B → **B+**. The copilot path used FastAPI `background_tasks` (reader-lifetime-bound, input-only, and double-extracting with route.ts); it now extracts in the `run_detached` `on_complete` hook like the named-agent path — fires on reconnect/cancel, includes the folded answer, via one shared `chat_fold.build_extraction_conversation`. route.ts's client-side `extractMemories` removed (dead). 4 new extraction-conv trajectory evals.
- 2026-07-02 — **A1/A2 remaining items closed** (watchdog + loop detection + P1-5/P1-6). Grades: A1 B → **B+** (monolith is the only major open item), A2 A− → **A** (no open items). New `orchestrator/watchdog.py`: `WatchdogPolicy` centralises all timeout values + the native tier-selection rule (was 3 inline env reads); `LoopDetector` trips a RUN_ERROR when a tool repeats with identical args (native path). P1-6 found already implemented (durable session store + retry) — doc corrected. P1-5: per-thread emit ordinal now rides the local `_stream_id` (`#<n>`); skip-by-count deferred (client delta-dedup + UI drive). 6 watchdog/loop trajectory evals. **A1+A2+D1 review substantially complete**: remaining are the executor monolith split, route history-assembly + live-fold demotion (UI-drive-gated), and the `lastEventId` skip optimization — all lower-impact.
- 2026-07-02 — **Score reconciliation + awesome-list SOTA comparison.** Propagated the closed items into adjacent grades: A3 A− → **A** (P0-3/P1-2 shipped, cursor plumbing in), C5 B+ → **A−** (P1-9 run-boundary extraction), D4 B → **B+** (durable session store + P1-2). Added the "State-of-the-art comparison" table above: the A1/A2/A3/A5 core is **at or above** the list's documented best practice on agent loop, event protocol, HITL, long-running + multi-worker correctness, and loop/stall detection — with the harness itself eval-locked (a practice the list preaches, few implement). Real remaining gaps are all OUTSIDE this core: C4 prompt caching (D, top cost lever), B6 sandboxing (C, top safety lever), C2 central context assembly (C+), E1 agent-quality evals.
- 2026-07-03 — **B6 (HH-6 near-term) shipped — the top safety lever.** Grade B6 C → **B−**. Audit confirmed the worst-case posture: Copilot-SDK agents ran with `PermissionHandler.approve_all` (every shell command / file write / network fetch auto-approved, no policy) at 5 executor sites, native-MAF had no permission concept, and the `tool_annotations` risk vocabulary was unused for gating. Fix: `acb_skills/permission_policy.py::risk_aware_permission_handler` (drop-in for approve_all, same `(request, invocation) -> PermissionRequestResult` signature) reads the SDK's OWN `PermissionRequest` classification (shell `full_command_text`/`commands`, `has_write_file_redirection`/`path`, `url`, `read_only`) + our risk annotations, and decides per a table: read-only/reversible tools approve; **destructive tools APPROVE-deferring to their own `request_confirmation` fail-closed gate (no double-gate/deadlock — the key composition insight)**; dangerous shell (rm -rf /, fork bomb, curl|sh, dd-to-device) and out-of-workspace writes fail CLOSED; network approves+logs; unknowns fail open-but-logged (near-term — tighten from observed data). Wired at all 5 sites via a mode-guarded `_copilot_permission_handler()` helper (`AGENT_PERMISSION_MODE` enforce|audit|approve_all — audit = safe rollout, logs would-be denials); native-MAF Tier-2 `_make_tool_shim` also gates+logs. Every decision logs with the E2 run correlation → privileged ops are now observable + attributable. 30 unit + 4 trajectory (decision-table contract); full suite 701 green. Deferred: native-MAF Tier-1 streaming choke point, and container isolation for normal runs (Phase 5, the deep-isolation residual). See [`permissions_sandbox_b6.md`](permissions_sandbox_b6.md).
- 2026-07-03 — **E2 Phases 3+4 shipped — the debug loop is now self-serve.** Grade E2 B+ → **A−**. Phase 3: `/debug/runs` diagnostics API (`apps/gateway/gateway/routes/debug.py`, registered in main.py) — `GET /debug/runs` (list, filter by agent/status/user/thread/since_hours, lean rows), `GET /debug/runs/{id}` (full trace + traceback), `POST /debug/runs/{id}/flag`; EXECUTIVE/AGENT-gated (traces hold content); extends the coarse `GET /agent/run/{id}/status`. Phase 4: `scripts/feature_check.py` — one-command live sweep (`health`/`debug_api`/`chat_maf`/`chat_copilot`) that drives the real endpoints, enriches each with its durable run-trace status, prints a pass/fail table (+ `--json`, `--only`, non-zero exit on fail), and on failure prints the exact `GET /debug/runs/{id}` + `journalctl` lines to debug; shares `CC_*` env with the CI-depth `tests/integration/test_chat_features.py`. Now: `feature_check` says WHAT broke + the run_id → `/debug/runs/{id}` gives the trace, no SSH needed. 9 debug-route integration tests (TestClient vs live DB) + 3 harness unit tests; full suite 667 green. Only E2 open item: OTLP trace-backend export (dormant). See [`observability_e2.md`](observability_e2.md).
- 2026-07-03 — **Review #4 (E2 observability, Phases 1+2) shipped — user-requested debugging capability.** Grade E2 C+ → **B+**. Goal: log every agent/model interaction so "error X happened with agent Y" is debuggable after the fact + groundwork for VPS feature-testing. Audit found logs were uncorrelated colored-console text (couldn't grep by run/agent) and the full run trace lived only 1h in Redis (latest-run-only) with no traceback anywhere durable. **Phase 1:** `acb_common.bind_run_context`/`clear_run_context` bind run_id/thread_id/agent/user into structlog contextvars at the executor run boundary → every line the run emits (incl. `acb_llm.usage`, which passes no ids) is auto-correlated via `merge_contextvars`; `LOG_FORMAT=json` renderer toggle for prod (env-driven, no code change); LLM usage audit rows now carry the run context + `agent:<name>` actor. **Phase 2:** new `agent_run` table (migration 50) + `gateway/run_trace.py` records one row per run at the boundary (reusing chat_fold's event replay) — metadata + tool summary for ALL runs, full trace + traceback ONLY for errored/cancelled/flagged (user-chosen retention: storage + privacy). Traceback now captured (`exc_info=True` log + `agent_run_error` payload). 11 unit tests; migration validated idempotent against live DB + E2E write/read-back; full suite 664 green. Designed-not-built: Phase 3 `/debug/runs` diagnostics API, Phase 4 one-command VPS feature-test harness (builds on the existing `tests/integration/test_chat_features.py`). See [`observability_e2.md`](observability_e2.md).
- 2026-07-03 — **Review #3 (C2 server-side context assembly) shipped.** Grade C2 B− → **A−**. Audit found the per-turn INPUT context was assembled client-side and trusted verbatim: the executor re-sliced the client-sent `messages[]` in SIX sites with different count caps (12/16/20/50) + char-truncation, all token-blind, and non-chat callers (API/webhook) got no history because nothing rebuilt it server-side. The token-accounting utilities in `acb_llm/context.py` (`count_message_tokens`/`fit_messages_to_context`) existed but were email-only. Fix: one `acb_llm.assemble_run_context` — token-budgeted windowing (drop-oldest-whole-turns until it fits, then char-trim the longest survivor; the two-stage fit converges on many-medium-turn chat transcripts the email-shaped single-message trimmer couldn't) + server-side current-turn dedup + DB-rebuild-when-empty via an injected `history_loader` (so `acb_llm` keeps no gateway dep). Wired into BOTH executor structured-message sites (`_compose_maf_run_input` streaming + the Tier-2 batch path) so they feed the model identically (kills the acknowledged duplication), and `agent.py` injects a `_get_messages`-backed loader for `thread_id`-carrying callers with no client history. Backward-compatible (browser clients still send `messages`; loader is a no-op then). 12 unit + 4 CI-blocking trajectory evals; full suite 646 green, zero regressions. Next core: B6 sandboxing.
- 2026-07-03 — **Review #2 (C4 prompt caching) shipped — the top cost lever.** Grades: C4 D → **A−**, C2 C+ → **B−** (session-scoped memory). ADR-008 implemented; plan Phases 1–6 done (`llm_caching_memory.md` Implementation-Status table). **Key architecture correction:** the platform is litellm-SDK-direct (NO proxy), so the plan's "proxy pre-call hook" doesn't apply — replaced by ONE `acb_llm/prompt_cache.py::apply_prompt_caching` transform called by both completion choke points (`acb_llm.complete*` for orchestrator-internal calls; `v1_compat._handle_chat_completions` for ALL agent traffic — native-MAF + Copilot SDK). It splits the system message at a `<!-- CACHE BREAK -->` sentinel (executor + `agents.py` inject it at the stable/dynamic memory seam) → Anthropic `cache_control` on the stable block; marks the last tool schema (tools-first caching for function-tool agents); adds OpenAI `prompt_cache_key`; strips the sentinel for DeepSeek/others so it never reaches the model (litellm 1.86.0 carries `cache_control` on OpenAI-format blocks → Anthropic, verified). Session-scoped memory (`acb_memory/session_cache.py`, injected-redis for layering) makes the memory block byte-stable across a thread's turns. Plus: LiteLLM Redis exact-match cache (`_init_litellm_cache`, opt-in), Graphiti episode worthiness gate, Anthropic startup prewarm (`main.py::_prewarm_prompt_cache`). 23 caching tests (5 CI-blocking trajectory); full suite 566 unit + all trajectory green, zero regressions. Caveat: explicit-cache win materialises only when a tier resolves to Anthropic/OpenAI (default DeepSeek self-caches); Phase 7 email tool-surface reduction is app-scoped + pending. Next core: C2 server-side context assembly, then B6 sandboxing.
