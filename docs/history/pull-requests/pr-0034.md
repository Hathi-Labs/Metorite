# PR #34 — docs: competitive analysis (Hermes/OpenClaw) + documentation reconciliation & consolidation

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:claude/hermes-open-claw-comparison-qnlgta` → `FracktalWorks:main` |
| **Created** | 2026-07-13T03:31:50Z |
| **Merged** | 2026-07-13T10:04:59Z |
| **Closed** | 2026-07-13T10:04:59Z |
| **Head commit** | `90f4abc2ab9fcc66bbf24bb520e3fbbfbb861e57` |
| **Merge commit** | `4d4db2a7f41f27ed80521da1c41976c1b4407597` |
| **Archived ref** | `refs/archive/pr/34` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/34 |

## Description

## Summary

Two connected pieces of documentation work, all **docs-only — no code changes**:

1. **Competitive analysis** of CommandCenter vs the two most-visible self-hosted agent platforms of 2026 (Nous Research's **Hermes Agent** and **OpenClaw**), with the learnings annealed into the existing backlog.
2. **A full reconciliation of the older documentation against the actual code**, then cleanup + consolidation, and a finalized "what's left" work plan.

The foundation audit docs (`FOUNDATION_AUDIT_REPORT.md`, `FOUNDATION_BUILDOUT_CHECKLIST.md`, `FOUNDATION_CONTINUATION.md`) were **left untouched** — they are the live foundation work tracker.

## Part 1 — Competitive analysis (`COMPETITIVE_COMPARISON.md`)

- Evidence-based three-way comparison. Headline: Hermes/OpenClaw are single-user personal agents; CommandCenter is an enterprise multi-agent orchestration platform — adjacent categories. We out-design both on paper (evals, governed writes, BYOK, human-gated self-mutation), but their *implemented* foundation (security enforcement, channels, ops robustness) is ahead of ours.
- Annealed into the backlog rather than left as a standalone doc:
  - New spec `ai-company-brain/specs/competitive_hardening_2026-07.md` (`CH-1..9`) attaching proven reference implementations to existing items (BO-1/5/7/12/14).
  - Two genuinely-new foundational items added to the checklist: **BO-20** (event-bus consumer + durable job queue, ref OpenClaw) and **BO-21** (memory activation + local-embeddings fallback, ref Hermes).
  - Phase-5 **Annealer** flagged as the home for self-improving skills (ref Hermes Curator).

## Part 2 — Documentation reconciliation & consolidation

Verified the whole older-doc corpus against the code (planning docs, ~27 specs, subtree AGENTS.md, README). Findings were systematically **stale-pessimistic** — most specs shipped past their own status.

**Stale references fixed (verified against the tree):**
- CopilotKit (removed M2.5, not in `package.json`) purged from README + planning AGENTS.md; flagged in `system_architecture.md`.
- Langfuse "removed" → corrected to dormant container behind `--profile obs` (→ BO-5).
- LiteLLM "proxy" → in-process SDK; `litellm/config.yaml` marked vestigial (→ BO-16).
- Deleted `acb_schemas` package references; Python 3.11 → 3.12; drift-prone version table → pointer to `uv.lock`; broken `level4/` xref; child-DOX-index gaps (acb_audit/acb_auth/email_ingestion/action_broker).
- Deleted `CCWORKBENCH_MEMORY.md` — orphaned session diary (no code references it) that also carried a hardcoded VPS IP + dev token.

**Specs consolidated: 27 → 14 living + 13 archived.**
- Shipped/historical specs moved to `ai-company-brain/specs/archive/` with a README indexing why each was archived and where residual open work went (email parity gaps → project_plan, runtime item ③ → BO-7, `github_search` bug → M9).
- Merged the email cluster (4→1) and chat cluster (3→1) into their living masters; core-architecture zoom-ins folded into `core_module_map.md`.
- Rewrote the specs index with statuses corrected against code; all internal links repointed (no broken refs).

**Work plan finalized into two clear surfaces.** `project_plan.md` and the planning `AGENTS.md` now carry a banner tying the **feature roadmap** (M3→M6) to the **foundation-hardening backlog** (`BO-*` / `CH-*` / `HH-*`), with P0 foundation work (BO-8/2/1/3 + SEC-1) marked as the active workstream gating L3/M5. Memory marked inert-by-default (→ BO-21), Action Broker's real (unwired) status surfaced (→ BO-1), Artifact Viewer marked shipped.

## Deliberately left / flagged
- Foundation audit + checklist docs — untouched (live tracker).
- `system_architecture.md` ADR history preserved with "superseded" banners (matching the doc's own ADR pattern) rather than rewriting the CopilotKit-era chat-architecture section — flagged for a future rewrite.

## Commits
- `docs: competitive analysis vs Hermes Agent and OpenClaw`
- `docs: anneal Hermes/OpenClaw learnings into the work plan`
- `docs: reconcile stale references across README/root/subtree docs`
- `docs: consolidate specs — 27 -> 14 living + 13 archived`
- `docs: reconcile planning docs vs code + finalize work-plan linkage`

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01GfyJCtJ5WWuXuhx21GngJZ

---
_Generated by [Claude Code](https://claude.ai/code/session_01GfyJCtJ5WWuXuhx21GngJZ)_
