# PR #317 — docs(board): WS-6 audited NO-GO — spec contract fails, board corrected

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:ws-6-audit-nogo` → `FracktalWorks:main` |
| **Created** | 2026-08-01T14:35:16Z |
| **Merged** | 2026-08-02T05:15:39Z |
| **Closed** | 2026-08-02T05:15:39Z |
| **Head commit** | `dff807762c22e510d455cd474abe24c3137bfe42` |
| **Merge commit** | `dec92c940666dd223fe38fa3d0c5db5cb3530625` |
| **Archived ref** | `refs/archive/pr/317` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/317 |

## Description

## Cycle result: NO-GO (no implementation attempted)

`/next-ticket` selected **WS-6 — Observability wiring + attribution (BO-5 / D1)** because it was the only 🟢 row the board names as a prerequisite for another (WS-16 Centers E budgets sits 🟡 behind it). `spec-auditor` returned **NO-GO**, so the cycle stopped before any code was written. This PR records the verdict on the board so the next cycle does not re-audit the same ticket.

## Why NO-GO — the doc contract, not the work

Against `work_plan.md` §1's seven points, `observability_e2.md` fails 5 of 7:

| # | Point | Result |
|---|---|---|
| 1 | Verified status header | FAIL — undated vs code; a load-bearing body claim is false |
| 2 | Scope / non-goals | FAIL — D1's attribution stamp appears in **no spec at all** |
| 3 | Per-item acceptance | **FAIL (decisive)** — zero `done when` in the whole file; the cited "§6.7" is an unnumbered recommendation memo |
| 4 | Current paths | FAIL — pre-restructure `apps/gateway/…` (4 sites) + `acb_common/_log.py` |
| 5 | Verification commands | PARTIAL — exist for shipped phases, none for §6.7 items |
| 6 | Single owner | PASS |
| 7 | Gate labels | FAIL — no AGENT-SAFE / OWNER-GATE marker anywhere |

## Drift found by reading the code (not the prose)

- **`agent_run.{prompt,completion,total}_tokens` are never written.** `run_trace.py::_persist_row` is the only `INSERT INTO agent_run` and omits them, while `/debug/runs` and `/observability/runs` faithfully SELECT and serve them. They are always NULL; the spec claims they carry cost attribution.
- **Durable cost storage does not exist for agents** — cost lives in Redis day-hashes (45-day TTL) plus an opt-in `audit_event`. The precedent to generalise is `app_audit` (mig 114), already backing a monthly budget in `routes/apps/runtime.py`.
- **Only 2 of D1's 4 fields reach the choke point.** `v1_compat` reads `x-cc-agent` / `x-cc-source`; run_id and member are dropped. `instance` exists nowhere — the vocabulary to reuse is `_resolve_agent_instance()` (`executor.py:917`, per mig 136), not a new key.
- Board's own claim confirmed precise: `opentelemetry-api/-sdk` are in `uv.lock`, but `opentelemetry-exporter-otlp` and `langfuse` are absent — so setting `OTEL_EXPORTER_OTLP_ENDPOINT` today buys nothing.

## Changes in this PR

- WS-6 row: 🟢 → **🟡 Docs**, carrying the verified drift, the unblock recipe, and the agent-safe vs owner-gated split.
- §6 owner-gate registry gains **observability activation**: Langfuse keys + `--profile obs` in prod, `OTEL_EXPORTER_OTLP_ENDPOINT`, `LLM_USAGE_AUDIT=1`, and re-enabling the MAF telemetry kill switch (`executor.py:114`) — it hides a known ContextVar-reset bug. An implementer reading §6 today would find nothing forbidding the Langfuse half.

Docs only — no code, no migration, no verification commands to run.

## What unblocks WS-6 (all AGENT-SAFE)

Add a numbered `## 7. WS-6 — open work` to `observability_e2.md` with per-item **done when** + gate labels, fix the 5 stale paths, and correct the token-column claim. The auditor supplied six testable acceptance bullets and the exact edits.

Then the dispatchable slice is **the D1 stamp + a durable `llm_call` table** (agent-safe): extend `bind_run_context` with `instance`, propagate `(run_id, member, agent, instance)` to `v1_compat` as fail-soft headers, widen `_emit_usage`, add the table modelled on `app_audit` with `cost_usd` **nullable** (never 0-for-unknown), and backfill the always-NULL `agent_run` token columns. Langfuse/OTel/`LLM_USAGE_AUDIT` stay owner-gated.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
