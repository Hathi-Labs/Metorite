# PR #391 — WS-26f: pipeline truth + settings UI + weighted pipeline (demo D1)

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:ws-26f-pipeline-truth` → `FracktalWorks:main` |
| **Created** | 2026-08-07T08:06:44Z |
| **Merged** | 2026-08-07T10:16:53Z |
| **Closed** | 2026-08-07T10:16:54Z |
| **Head commit** | `4ad13f93303eeafe4c2e43a3ca987cf38e7510da` |
| **Merge commit** | `622a1b88e7b2dda31886c3d16870c2bf824b47d8` |
| **Archived ref** | `refs/archive/pr/391` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/391 |

## Description

WS-26f — demo critical path D1. Full chain: spec-audit GO-NARROWED (C1–C8) → build → independent verify (all criteria met, 4 mutants re-run red) → docs correction (6051be70) → adversarial review (in flight; merge waits for it).

- **f1** `POST /crm/import/zoho/stages` — floor `admin:access:manage`, dry-run by default, `?apply=true` owner-gated; multi-pipeline ⇒ STOP before the DB opens; no-scope vs no-data vs version-refusal all distinguishable, scope string named.
- **f2** `?tab=settings` — three grids (deal stages drag-reorder ×10 renumber, lead statuses, lost reasons) over the existing admin API; won=100/lost=0 clamp both sides (server 422 + client message, word-identical).
- **f3** weighted ₹ — whole-lane SQL aggregate (`COALESCE` onto the stage default, open/ongoing only, `on_hold` excluded by name), header rollup, editable probability on the record sheet.
- **f4** `closed_at` proxy backfill — direct UPDATE, statically + behaviourally pinned to bypass dirty-marking (zero Zoho pushes queued).

456 backend + 618 frontend tests green; theme conformance clean; no migration needed. Running the pull against the live tenant (dry run included) has NOT been done — that is the registered owner gate.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
