# PR #115 — refactor(email): Phase 2 batch 1 - safe convergence (embeddings/attachments/health)

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:refactor/email-phase2-converge` → `FracktalWorks:main` |
| **Created** | 2026-07-22T10:53:46Z |
| **Merged** | 2026-07-22T11:04:21Z |
| **Closed** | 2026-07-22T11:04:21Z |
| **Head commit** | `48d8b85367d442afa0aceab4f1e9beb80c4829f0` |
| **Merge commit** | `669424d52b52296071fc3e6172256f8dbda1872d` |
| **Archived ref** | `refs/archive/pr/115` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/115 |

## Description

## Phase 2 — batch 1: safe convergence

First slice of Phase 2 ("converge the seams") from `email_app_master_plan.md`. These three kill defect *classes* without touching the live auth / sync / classification paths — deliberately the low-risk batch to validate the Phase 2 approach on CI + the live account before the structural refactors (2.1/2.2/2.3) land.

| # | Fix | Class it kills |
|---|---|---|
| 2.5 | Embedding sweep's `content_hash` now byte-matches its SQL candidate predicate (`_hash_source`, no strip; `convert_to(...,'UTF8')`) | The sweep re-embedded every message with surrounding whitespace **every tick, forever** — semantic search was shipped-but-unlaunchable. Prod flag stays OFF; this only makes it correct when enabled. |
| 2.8 | `email_attachments` UNIQUE `(message_id, provider_attachment_id)` + dedupe migration (88); all 3 inserts name the arbiter | Bare `ON CONFLICT DO NOTHING` was dead (only unique key was the always-fresh `id`), so re-hydration duplicated attachment rows without bound. |
| 2.12 | `#110` damage definition promoted from the one-off #112 repair script to a counted health metric (`data_health.damaged_threads`), shared SQL, amber banner only when non-zero | A regression alarm no one has to remember to run; the alarm and the fix can't drift (one SQL constant). |

### Verification
- New unit tests: `test_email_embeddings_hash` (parity, incl. whitespace/unicode), `test_email_attachment_dedupe` (all 3 sites name the arbiter + migration shape), `test_email_thread_health` (scoping + no second SQL copy). Analytics suite updated.
- Blocking lint clean; frontend `tsc --noEmit` clean.

### Deferred / notes
- Semantic-search prod enable (`email_semantic_search_enabled`) is a separate product decision — **not** flipped here.
- No live behavioral change on the daily-driver paths; migration 88 is idempotent (dedupe DELETE + `CREATE UNIQUE INDEX IF NOT EXISTS`).

Generated with Claude Code
