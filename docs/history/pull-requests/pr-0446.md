# PR #446 — fix(WS-27bj): three migration references the renumber got wrong, one of them mine

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:claude/multi-tenancy-ai-metering-nr8zbj` → `FracktalWorks:main` |
| **Created** | 2026-08-14T20:44:22Z |
| **Merged** | 2026-08-14T20:51:44Z |
| **Closed** | 2026-08-14T20:51:44Z |
| **Head commit** | `09b0c3d0937cb9f633109c7158aaa0b8f374dfc1` |
| **Merge commit** | `90a9d22d5b4c1884a4853987165cfbb5377c370c` |
| **Archived ref** | `refs/archive/pr/446` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/446 |

## Description

Found by a **concurrent sibling session** (#443) reviewing the renumber I did in #439. It was right and I was wrong, so this lands the correction rather than arguing with it.

Supersedes #443's first two changes; its board edit targets an anchor #444/#445 has since rewritten, so it would conflict. **#443 can be closed once this lands.**

## 1. `routes/tasks/people.py:642` — I renumbered this and should not have

The comment reads *"The vocabularies migration … deliberately did NOT put in a CHECK (D-PC-8/P-6)"*. That is about **People's** vocabularies — `employment_type` and `seniority`, which `172_people_profile.sql` adds without a CHECK over live data, and whose header carries exactly that D-PC-8/P-6 reasoning.

Verified rather than assumed: `175_projects_org_vocabularies.sql` does not mention `gtd_people` once. Back to **172**, and the comment now names the file so the next sweep can't repeat it.

**The cause is worth naming.** I renumbered by matching the *string* `"migration 172"` across the tree, when the question was which *file* each sentence described. A blanket rename is the wrong tool when two migrations briefly share a number — that window is precisely when the number is ambiguous.

## 2 + 3. Two references I missed

- `specs/project_management_app.md` still named `172_projects_org_vocabularies.sql` — **a file that exists on no branch**, and the first reference anyone reaches for when picking the ticket up.
- `work_plan.md` WS-27bj slice 1, same stale name.

Both are the mirror-goes-stale-then-lies failure CLAUDE.md §5 names.

## Also corrected

I wrote the collision was *"the third in this plan"*. R1's own header in CLAUDE.md already counts three, so this one is the **fourth**.

## Full audit

Every remaining `migration 17x` reference in `apps/`, `tests/` and `project-docs/` checked against the file it actually describes:

| # | File |
|---|---|
| 170 | `projects_search_trgm` |
| 171 | `projects_run_state` |
| 172 | `people_profile` |
| 173 | `people_avatar` |
| 174 | `people_absences` |
| 175 | `projects_org_vocabularies` |

All consistent.

## Verification

```
uv run pytest tests/unit/test_projects_org_vocabularies.py \
              tests/unit/test_projects_vocabulary_scope.py \
              tests/unit/test_people_profile.py \
              tests/unit/test_migration_prefixes.py \
              tests/unit/test_projects_run_state.py -q
241 passed
```

No SQL change; `175_projects_org_vocabularies.sql` is unchanged and already applied on the box.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01TCncTJFRG1LJqQ5zt395hS

---
_Generated by [Claude Code](https://claude.ai/code/session_01TCncTJFRG1LJqQ5zt395hS)_
