# PR #392 — WS-26d-email: caller-scoped email threads on CRM timelines (demo D2)

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:ws-26d-email-timeline` → `FracktalWorks:main` |
| **Created** | 2026-08-07T08:06:46Z |
| **Merged** | 2026-08-07T09:04:26Z |
| **Closed** | 2026-08-07T09:04:26Z |
| **Head commit** | `0aa30dec6f72b51cb56954a4fc5a8a568dd6a37d` |
| **Merge commit** | `ded21ef7dc01c3851784fa66d1914fdd2645a91f` |
| **Archived ref** | `refs/archive/pr/392` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/392 |

## Description

WS-26d-email — demo critical path D2. Full chain: spec-audit GO-NARROWED (C1–C13, mutant fence mandatory) → build → independent verify + adversarial review (in flight; merge waits for both).

- Caller-scoped email threads on CRM record timelines: the timeline shows email *the caller can already read*, never email *the record has*. The predicate is a verbatim copy of the email app's `_account_scope` (D-CRM-4: copy, don't import), aliased `em`, uid pre-seeded.
- Thread-grained (`account_id, thread_id`), newest message per thread, status badge via `email_thread_status`; deals reach email through lead + deal-contacts, unioned; orgs never join by domain; inbound only.
- **Security proven by mutation, not assertion**: with the scope predicate deleted, the no-mailbox and two-holder isolation tests go red with the leak visible; restored byte-identical. The CRM test fakes were extended so the scoped subquery is actually evaluated (the old readers silently mis-parsed it).
- New sender-address index migration (number measured at build time); `TimelineEntry` gains `email_thread` kind + payload on both sides (closed TS union — one-sided edits fail tsc); third render branch (the old binary dispatch would have thrown at runtime).

520 backend + 591 frontend tests green.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
