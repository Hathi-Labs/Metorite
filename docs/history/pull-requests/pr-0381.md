# PR #381 — docs(WS-26): demo critical path — re-sequence the CRM plan for demo-readiness

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:ws-26-demo-critical-path` → `FracktalWorks:main` |
| **Created** | 2026-08-07T05:14:47Z |
| **Merged** | 2026-08-07T05:14:54Z |
| **Closed** | 2026-08-07T05:14:54Z |
| **Head commit** | `e271b7ccbe4f28b94b22ceaa1127599add7abd5d` |
| **Merge commit** | `727c1f642d4a632f2fb2c868bf312bc42472c67b` |
| **Archived ref** | `refs/archive/pr/381` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/381 |

## Description

Owner directive: get the CRM demo-ready as soon as possible **without compromising the plumbing**.

- **spec §9.0 (new): the demo critical path** — a strict dispatch order over the existing tickets: **D1 WS-26f** (the board is the first screen) in parallel with **D2 WS-26d-email** (disjoint files) → **D3 WS-26g** (after f: shared tab grammar in `page.tsx`/`urlState.ts`) → **D4 WS-26d-write** → **D5 WS-26d-autolead** (flip stays owner-gated). WS-26h/i/e deferred past the demo — deferred, not demoted.
- **Nothing is thinned**: every slice keeps the full audit → implement → verify → review chain, every done-when, every §6 gate.
- **WS-26f gains f4**: imported won/lost deals carry no `closed_at` (only native transitions stamp it), so win/loss, cycle-time and won-₹ would demo empty. f4 backfills it from Zoho's `Closing_Date` as a *labeled proxy*, via a direct UPDATE that **bypasses dirty-marking** — through the writer it would queue ~500 no-op pushes into the live tenant. Done-when 4 now pins exactly that.
- **Owner pre-demo runbook** in §9.0: approve the f1 apply (dry-run first), merge slices as they land, grant `feature:crm` to the room, optionally flip `CRM_AUTO_LEAD`.
- Honesty notes for the demo: funnel/dwell start sparse (native transitions only — that's the instrument working), proxy close dates are labeled as such.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
