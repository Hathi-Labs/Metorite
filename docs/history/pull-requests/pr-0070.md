# PR #70 — Task manager: Done button, sidebar counts, clarify UX, smart capture, sync visibility

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:feat/task-manager-polish` → `FracktalWorks:main` |
| **Created** | 2026-07-17T17:25:59Z |
| **Merged** | 2026-07-17T17:49:39Z |
| **Closed** | 2026-07-17T17:49:39Z |
| **Head commit** | `a42a84f5284b0b38b2eeb66c0ef767fea1d839f1` |
| **Merge commit** | `ed07b3a2c6a784be201ead77b7ce1c520665218d` |
| **Archived ref** | `refs/archive/pr/70` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/70 |

## Description

Overnight batch of task-manager fixes + UX improvements. **Draft — for morning review, nothing merged to `main`.** One branch, one commit per item.

## What's in here (Tier 1 + Tier 2, minus the hotkey)

| # | Commit | What changed |
|---|--------|--------------|
| 5 | `fix: sidebar counts track source toggle` | `viewCounts` ignored `sourceFilter`, so the All/Mine/ClickUp badges were frozen at the "All" totals while the list re-filtered. Threaded `source` through (mirrors `itemsForView`). |
| 1 | `feat: one-click Done` | Cards had no complete affordance (only the status dropdown). Added a leading circular check on both card variants + a prominent Done/Reopen button in the detail header, wired to the existing `quickDispose(id, DONE)`. |
| 2 | `feat: declutter priority + suggestion badges` | Two colored pills competed on every card. Card face now skips the pill for the default low-priority cell and renders the suggestion nudge as a quiet neutral chip. Detail Priority section keeps full color/info. |
| 11 | `feat: editable title + assistant context in clarify` | Title is now inline-editable (was only changeable via a hidden vague-title banner). New collapsible "Add context for the assistant" field → posted to `/clarify` as an optional note, threaded into `_llm_propose` as trusted user guidance so it re-derives title/project/steps. **Backend is additive; GTD golden-eval trajectory still 21/21.** |
| 12 | `feat: tighten clarify Shape step` | `SubField` gained an `inline` variant (label left of control on wider screens); applied to the six compact pill rows, roughly halving the Shape step's height. |
| 13 | `feat: detect multi-task single captures` | A single quick-add that reads like several actions is now run through the existing atomizer before filing; if it splits into >1, the mind-sweep review opens pre-populated (with a "Keep as one" escape hatch). Short/dated/attached captures file instantly as before. |
| 6 | `feat: surface ClickUp sync status in sidebar` | Each sidebar workspace row now shows last-synced time / sync error and a one-click sync button (was buried in the Workspaces modal). |

## Item 6 — audit answers (the questions this item asked)
- **Webhooks vs schedule?** The task-manager mirror (`gtd_items`) is refreshed by the **periodic scheduler + manual sync only**. A signed ClickUp webhook *does* exist but feeds the **knowledge graph**, not the task DB.
- **Manual sync button?** Already existed (Workspaces modal); this PR makes it accessible from the sidebar too.
- **Two-way project sync?** Task data is two-way. Project **structure** write-back is partial — creating a list propagates upstream, but spaces/folders and structural edits stay local.

## Deferred (per our plan)
- Hotkey-to-add-for-agent (item 8), Tier 3 (delegate-to-agent 7, approvals/notifications hub 9, people-waiting 10), Calendar (3) + Pomodoro (4), WhatsApp, voice, and the ClickUp **deep** sync (task-DB webhook receiver + full structural write-back).

## Verification
- `tsc --noEmit`: **pass** (full project).
- `apps/.../tasks/ai.py`: `py_compile` clean; `pytest evals/trajectories/test_gtd_quality_trajectory.py` → **21 passed**.
- Frontend eslint: clean on all changed lines (one pre-existing `set-state-in-effect` error remains in untouched `ItemDetail.tsx:172`).
- Not exercised in a running app (no dev server this session) — worth a quick click-through of: Done toggle, sidebar counts on toggle, clarify title edit + "Add context", and a multi-task quick capture.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
