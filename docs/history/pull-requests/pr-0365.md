# PR #365 — feat(WS-26): the CRM's sync engine and UI - 26b + 26c, concluded and cross-checked

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:ws-26-crm-app` → `FracktalWorks:main` |
| **Created** | 2026-08-06T06:19:29Z |
| **Merged** | 2026-08-06T06:21:33Z |
| **Closed** | 2026-08-06T06:21:33Z |
| **Head commit** | `7881b8db803535af091b11b2c3e272d44f7ab9f7` |
| **Merge commit** | `8d83ca10ed24b20f12bfc510a6d2d84c07a73ca7` |
| **Archived ref** | `refs/archive/pr/365` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/365 |

## Description

## What this lands

PR #362 was merged at its WS-26a head, so main currently carries the CRM schema and engine seam only. This PR carries the rest of the built stack — the two slices the owner authorized under D-CRM-7, already reviewed and merged into this branch via #363 and #364:

- **WS-26b — two-way Zoho sync** (`sync_zoho.py`, `writer.py`, migration `145_crm_zoho_sync.sql`): dirty-tracking push queues, tombstoned deletes, cursor-snapshot pulls with echo suppression, per-record transactions with retry ceilings, broker-gated `crm.zoho_*` pushes. Ships **OFF** behind `CRM_ZOHO_SYNC`.
- **WS-26c — the CRM UI** (`workbench/control_plane/src/app/crm/`): pipeline board, leads/contacts/organizations collections, convert flow, timeline, URL-as-state views; BFF proxy + nav/access/centers registration.
- The 26b×26c union merge (`7881b8db`) resolving six cross-slice conflicts, with the import-path primary-rule fence test covering both slices.

## Verification (on this exact head)

- `uv run pytest` on the 8 named CRM/gateway test files: **475 passed**
- control_plane: **tsc clean, 281 vitest passing**
- correctness-ruff clean

## What this does NOT do

Nothing activates on deploy: `CRM_ZOHO_SYNC` is off, `feature:crm` is `is_default false`, backfill is a separate owner-gated run. The §6 owner gates in `work_plan.md` govern every flip.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
