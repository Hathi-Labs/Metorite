# PR #150 — fix(email): review follow-ups — gate-probe rollbacks + attachment download on provider_session

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:review-followups-provider-seam` → `FracktalWorks:main` |
| **Created** | 2026-07-22T18:21:15Z |
| **Merged** | 2026-07-22T18:25:01Z |
| **Closed** | 2026-07-22T18:25:01Z |
| **Head commit** | `c0ddec86bc78bc9a087d625d5203b21dc5e94ce6` |
| **Merge commit** | `f41afdf23e3c655f4a94fa50a323d01085266af3` |
| **Archived ref** | `refs/archive/pr/150` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/150 |

## Description

Post-Phase-2 session review (3 parallel audit agents over merged main, findings hand-verified). Two DEFECT-class findings fixed; the rest of the review lands as master-plan updates in this same PR.

1. **Auto-learn gate probes poisoned the session on failure** — both best-effort probes in `automation/learning.py` caught DB errors without `rollback()`, immediately upstream of the audit-row INSERT on the same session. Same `InFailedSQLTransaction` class as the `/digest/send` 500 (#148). Now: rollback + warning log, behavior (fail-closed / fail-open directions) unchanged.

2. **`download_attachment` was the last raw provider call site** — no `authenticate()` (expired access token = hard 401 for the user) and no rotated-cred persist (refreshed token dropped). Converted to `provider_session` (account-scoped, auth required) + `db.commit()` to land the staged credential update; hand-rolled decrypt/instantiate removed.

Tests: 2 rollback pins + new `test_email_attachment_download.py` (3 seam pins). 

🤖 Generated with [Claude Code](https://claude.com/claude-code)
