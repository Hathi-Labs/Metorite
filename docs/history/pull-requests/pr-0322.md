# PR #322 — docs(board): WS-5 audited NO-GO — CI's blocking gates are decorative (main has no branch protection)

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:ws-5-audit-nogo` → `FracktalWorks:main` |
| **Created** | 2026-08-01T16:44:13Z |
| **Merged** | 2026-08-02T05:17:32Z |
| **Closed** | 2026-08-02T05:17:32Z |
| **Head commit** | `6f2ea3b3f24c1ac76efbac63bd13ec00d17bbf10` |
| **Merge commit** | `c7bd0f48963f49b50d81568f1cc4e4f87f545457` |
| **Archived ref** | `refs/archive/pr/322` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/322 |

## Description

## Cycle result: NO-GO (no implementation attempted)

`/next-ticket` selected **WS-5 — CI gates real (BO-17/BO-18)** because the previous cycle produced live evidence it was load-bearing. `spec-auditor` returned **NO-GO** on contract point 3 — `FOUNDATION_BUILDOUT_CHECKLIST.md` §F has **zero testable done-whens** ("per the existing plan", "a few green PRs", "for foundation packages"). This PR records the verdict so the next cycle doesn't re-audit it.

## ⚠️ The finding that matters — read this even if you skip the rest

**`main` has no branch protection.** `gh api repos/FracktalWorks/CommandCenter/branches/main/protection` → `404 Branch not protected`. No required status checks, no PR requirement. **Every "blocking" step in `pr-check.yml` currently gates nothing.**

That's the root of the F821 escape, and the audit reconstructed the full chain:

| # | Hole | Evidence |
|---|---|---|
| 1 | No branch protection | API returns 404 |
| 2 | Direct pushes to main get **zero check-runs** | `gh api .../commits/15c8933f/check-runs` → empty. A push runs workflows only for the head SHA, so that commit got no CI of any kind |
| 3 | `deploy.yml` runs **no correctness gate** | `:56-58` is `ruff check .` with `continue-on-error: true` — the non-blocking *full* check, not the `--select F821,…` selection. "Lint & type-check" reported success over a broken tree |
| 4 | A red PR merged anyway | **PR #318's `pr-check` failed on this exact F821** (run 30704151709, step "Ruff correctness (blocking)") and merged as `bc1e7d97` |

A weaker fourth link: `.pre-commit-config.yaml` exists but `.git/hooks/pre-commit` isn't installed in this clone — pre-commit is per-clone opt-in and can never be a gate.

**Main is still red right now.** PR #320 fixes it and is green on all four checks, but nothing forces it to merge before the next deploy.

## Two long-standing beliefs, refuted

Both were in my working notes and both are **wrong**:
- ~~"pr-check is Python-only; `workbench/control_plane` gets no CI"~~ → there **is** a `frontend` job (`pr-check.yml:108-130`): `npx tsc --noEmit` + `npm test`. (Not `next build` — that stays deploy-time.)
- ~~"pr-check only runs against main, so stacked PRs get zero CI"~~ → the `branches:` filter was **deliberately removed** with that rationale in-comment. Residual is much smaller: `paths-ignore: ["**.md", "ai-company-brain/**"]` still yields a zero-check PR for docs-only changes (which is why *this* PR will show no checks).

## Other drift found

- BO-17 reads `☐ not started` while **half of it shipped**: blocking ruff-correctness (`:50-51`), blocking xenon ceiling (`:66-67`), the frontend job, `secret-scan`, per-PR codebase-health.
- The ratchet-plan anchor is broken — `ai-company-brain/specs/dev_velocity_tooling_2026-07.md` **does not exist**; it moved to `specs/archive/`. Three citations of the dead path live **inside the workflow files** (`pr-check.yml` ×2, `codebase-health.yml` ×1).
- Full `ruff check .` is now **1,968** findings; CI comments still say "~1.3k".
- Coverage: `pytest-cov` is a dev dep, but **nothing** measures coverage anywhere. `README.md:192` advertises a coverage command CI never runs.

## Changes in this PR

WS-5 row: 🟢 → **🟡 Docs**, and its blanket **"AGENT-SAFE" corrected to "mixed"** — the highest-value item is a GitHub *settings* change an agent cannot make and must not pretend to.

## What unblocks WS-5

Add testable done-whens to §F, restate BO-17 against code, fix the archive anchor, and **write down BO-17a — the missing item** (the escape chain above). Then the agent-safe slice is: a `correctness` job on push-to-main running the `--select` gate, deliberately **not** in the deploy job's `needs:` — loud, not blocking. Acceptance is self-evidencing: re-run `deploy` at `822d0610` and the job fails while deploy still runs.

**OWNER-GATE, explicitly:** enabling branch protection / required checks, wiring any gate into `needs:` (can block production deploys), and removing `skip_tests` (deliberate hotfix scar tissue). BO-18's history purge + Zoho rotation is **WS-2's**, not this row's.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
