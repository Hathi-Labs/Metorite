# PR #32 — Move avatar assignment to Agents page + mobile office fixes

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:feat/agent-avatar-picker-mobile-fixes` → `FracktalWorks:main` |
| **Created** | 2026-07-12T08:34:02Z |
| **Merged** | 2026-07-12T09:12:24Z |
| **Closed** | 2026-07-12T09:12:24Z |
| **Head commit** | `7d7951a090f25d4c884f1e10a4d7daa96d23a1a4` |
| **Merge commit** | `6dc0f42bbf4427e61779bcd40f1cd30a99bb09ec` |
| **Archived ref** | `refs/archive/pr/32` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/32 |

## Description

## Summary

Addresses four things from the mobile screenshot + avatar-config feedback:

1. **Mobile — office cut off by the bottom nav.** The office scroll area extended behind the fixed bottom "Menu" bar. Added bottom clearance (`pb-24 sm:pb-2`) so the last row of desks / the model-server rack clears the bar (+ iOS safe area).
2. **Tab bar too tall.** Compact, horizontally-scrollable tab pills (`py-1`, smaller padding) — less vertical height.
3. **Removed Avatar Studio from the observability app** and deleted the now-unused code: `avatar-studio.tsx`, `sprites.generated.ts`, and the procedural `AgentScene`/`deriveAvatar`/`spriteFor` machinery in `scene.tsx` (slimmed to just `roleFor()` + the `AvatarConfig` type the office still needs).
4. **Avatar picker on the Agents config panel.** Selecting an agent now shows an **Avatar** section: broad role categories (**All / Designer / Engineer / Executive / Sales**) and a grid of the generated library characters, each previewing its **breathing animation**. Picking one saves `config.libraryId` via the existing `/observability/avatars` endpoint; the office then renders that character with its full seated/typing/sleeping/breathing set (`castFor`). A **Default** tile clears the assignment.

## Verification

- `tsc --noEmit` clean; `next build` green.
- Mobile screenshot: office scrolls fully and clears the Menu bar; tab bar compact; Avatars tab gone; 0 console errors.
- Agents screenshot: picker renders all 8 library characters (breathing), category chips, and the Default tile.
- No frontend eslint regressions from the new code (CI lint is Ruff-only; `next build` tolerant).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
