# PR #254 — Fix migration 119 collision and link the live console

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:claude/ai-note-taker-architecture-clfh8q` → `FracktalWorks:main` |
| **Created** | 2026-07-27T04:40:33Z |
| **Merged** | 2026-07-27T04:40:39Z |
| **Closed** | 2026-07-27T04:40:39Z |
| **Head commit** | `b0fd2edceecdd23ec998db0a59e00501de335f25` |
| **Merge commit** | `d675793ca517b81daa8da38e94ae8bbe9bba5865` |
| **Archived ref** | `refs/archive/pr/254` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/254 |

## Description

Unblocks the deploy for #253 and makes on-page live transcription reachable.

## 1. Migration prefix collision (blocking)

`119_email_contacts.sql` landed on main concurrently with `119_live_session.sql`, so both claimed 119 and `test_migration_numeric_prefixes_are_unique` failed — which skipped the deploy job entirely.

Renumbered to `120_live_session.sql`. The table is unchanged; only the filename, its header comment, the test's path and a spec reference move.

## 2. The live console had no way in

`LiveDock` deliberately hides your *own* local recording, since `RecordingDock` already covers it — which meant nothing actually pointed at `/notes/live/<id>` for an on-page recording. Added an **"Open the live console"** link under the captions on the recording screen: the captions there are this device's draft, the console is the server-side view with speakers kept consistent and named.

Migration guard + live-session tests pass; `tsc` and eslint clean.

---
_Generated by [Claude Code](https://claude.ai/code/session_016xdbRumGveUBFRsPh3Duu8)_
