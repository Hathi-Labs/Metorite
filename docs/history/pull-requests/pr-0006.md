# PR #6 — fix(email): correct OAuth post-auth redirect (404 after reconnect)

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:fix/oauth-redirect-after` → `FracktalWorks:main` |
| **Created** | 2026-06-20T06:07:16Z |
| **Merged** | 2026-06-20T06:08:15Z |
| **Closed** | 2026-06-20T06:08:15Z |
| **Head commit** | `5dab82057eb16ea92b2597bea2c0e0e77efbabd1` |
| **Merge commit** | `abf494fede7c1140314554673af9e64398f8597d` |
| **Archived ref** | `refs/archive/pr/6` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/6 |

## Description

## Why
After the reconnect flow added in #5, completing OAuth redirected to
`/email/oauth/https%3A%2F%2Fcommandcenter.fracktal.in%2Fintegrations` → 404.

`redirect_after` was **double-encoded** (`encodeURIComponent` + `URLSearchParams`), decoded only once downstream, so the callback pushed an encoded string as a relative path.

## What
- `integrations` EmailTab: pass `window.location.href` raw (URLSearchParams encodes once).
- OAuth callback page: normalise `redirect_after` to a safe same-origin path via `new URL()`, reject cross-origin/garbage (open-redirect guard), fall back to `/email`.

The account reconnect itself already worked — verified on prod: `sync_status=idle`, 100 messages now under `inbox`. This only fixes the cosmetic post-auth 404.

## Verification
`tsc --noEmit`: clean on both edited files.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
