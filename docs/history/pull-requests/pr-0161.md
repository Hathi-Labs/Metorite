# PR #161 — Chat & agent framework review: audit docs + P0–P2 chat fixes + orchestration Phase 0

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:claude/chat-agent-framework-review-qvimev` → `FracktalWorks:main` |
| **Created** | 2026-07-22T20:00:15Z |
| **Merged** | 2026-07-22T20:03:37Z |
| **Closed** | 2026-07-22T20:03:37Z |
| **Head commit** | `558e2bce71a9691fdf83caff409ace24cb534aa7` |
| **Merge commit** | `689aafb355ef8b1bf313c3cd45ace27caeddab92` |
| **Archived ref** | `refs/archive/pr/161` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/161 |

## Description

## Summary

Full review + hardening pass over the single-agent chat path and the agent framework, in three layers:

**1. Review & audit docs** (`ai-company-brain/specs/`)
- `chat_agent_framework_review_2026-07.md` — dual-runtime verdict (MAF as the framework, Copilot SDK as a coding engine only), orchestration/memory/artifact/HITL/co-authoring assessment, prioritized plan.
- `single_agent_chat_bug_audit_2026-07.md` — previously-encountered issues verified fixed (BYOK false context-overflow, telemetry RUN_ERROR, disappearing HITL cards, Copilot dedup, no-text-end, 402s); 7 new confirmed bugs found, all fixed in this PR.

**2. Single-agent chat fixes (audit P0–P2)**
- **Tier 1 loop-trip crash**: deterministic `AttributeError` on every LoopDetector trip → guarded; loop RUN_ERROR is now the terminal event.
- **Copilot retry duplication**: mid-stream provider/session errors no longer replay the whole turn (emitted-output guard); genuine stale resumes still recover.
- **Context management for BYOK/LiteLLM**: `/v1` now fits prompt + completion into the model's real window (shrink output → evict oldest turns incl. orphaned tool results → char-trim), closing the unbounded resumed-Copilot-session overflow; small-window rescue math fixed; system prompt protected from char-trim; `finish_reason=length` surfaced as a visible truncation notice; context pressure surfaced as a progress line.
- **Delivery pipeline**: control-bus applied-acks for cross-worker HITL answers and Stop (no more success-reported-but-undelivered); ACTIVE-flag heartbeat during HITL parks; relay MAXLEN 10k→50k (env-tunable) + head-trim warning at the persistence fold; Copilot stall detector gains a tool-in-flight grace tier (600s).
- **Session continuity by capability**: restore/save/`working_directory`/`ask_user` wiring now key on Copilot capability instead of the registry runtime label — mislabeled `"maf"` Copilot agents (task-manager, apis-config) resume sessions again instead of re-injecting history every turn.

**3. Orchestration Phase 0** (`multi_agent_orchestration.md` — the email hand-off root cause)
- `call_agent` family joins the guaranteed tool floor (`_CORE_STANDARD_TOOL_NAMES`).
- The injected-tools addendum is scope-aware: it describes only the tools an agent actually has (per-variant cache keeps each prefix byte-stable for KV-cache hits).
- Unknown `tool_scope` entries warn (`executor.tool_scope_unknown_entry` — catches the live `ask_user` no-op typo).

## Testing

- **1592 unit tests passed, 0 failed** (6 skipped, DB-bound) after merging latest `main`.
- **118 trajectory evals passed** (watchdog, HITL, chat fold, context assembly, prompt cache, addendum drift, tool scope, stream replay).
- **~55 new regression tests** across `test_run_agent_stream_e2e.py`, `test_v1_context_fit.py`, `test_control_ack.py`, `test_hitl_heartbeat.py`, `test_hitl_stall_suppression.py`, `test_context_trim_protections.py`, `test_v1_truncation_notice.py`, `test_tool_scope_addendum.py`, `test_core_tool_floor.py`.
- CI-blocking ruff selectors clean on all changed files.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01K9rzBFbJiEtwSyq8u7sWWt

---
_Generated by [Claude Code](https://claude.ai/code/session_01K9rzBFbJiEtwSyq8u7sWWt)_
