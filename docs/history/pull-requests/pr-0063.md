# PR #63 — feat: HR-aware task manager (Phases 1-4) + Rapid Inbox / historical-apply email fixes

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:feat/rapid-inbox-and-historical-apply` → `FracktalWorks:main` |
| **Created** | 2026-07-17T06:21:21Z |
| **Merged** | 2026-07-17T06:21:41Z |
| **Closed** | 2026-07-17T06:21:41Z |
| **Head commit** | `a0b54ae486c0f8106bc806a2ae670d6fb78bedc4` |
| **Merge commit** | `0efd385666a5f9c9a241a7f8acbca93d3426bc77` |
| **Archived ref** | `refs/archive/pr/63` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/63 |

## Description

Two workstreams, validated together. **All 21 GTD golden-trajectory evals pass**; the full `tests/unit/` suite is green; `next build` + `tsc` clean.

## Email fixes (commit 1)
- Rapid Inbox sections were empty (label queries were INBOX-scoped → now `folder:"all"`).
- Category order Reply · Task · Awaiting · Done · FYI.
- Cards open the full thread in the inbox.
- Uncategorized-pill rerun patches the in-memory list so the pill clears.
- **Historical apply downloads the requested range from upstream first**, then applies (so a range predating the local sync still has mail). Adjusts the tracker to a `downloading` phase; unit test updated to lock the new contract.

## Task-Manager HR epic — Phases 1-4 (commit 2)
- **P1** migration 74 (editable `gtd_people` + `gtd_person_resumes`) + résumé ingest (PyMuPDF/docx) + PeopleView/PersonEditor.
- **P2** migration 75 (`capability_embedding`, flag `task_semantic_match_enabled`, default **off**) semantic re-rank (never drops keyword) + workload/overload + `reports_to` reasoning (route-only; `propose()` untouched) + ClickUp people reconcile.
- **P3** `gtd_plan_project` + `planning.py` (plan → apply local/ClickUp); ClickUpProvider 429 back-off + `due_date_time`.
- **P4** `task_memory.py` dedicated clarification memory on the `agent:task-manager` Mem0 scope + framework-doc §8 recipe.

## Deploy notes
- Migrations **74 + 75** auto-apply; new gateway deps **pymupdf + python-docx** install via `uv sync`.
- `MEM0_ENABLED=true` on the VPS → Phase 4 clarification memory is live immediately.
- Semantic matching stays **off** until `TASK_SEMANTIC_MATCH_ENABLED=true` (keyword is the default/fallback).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
