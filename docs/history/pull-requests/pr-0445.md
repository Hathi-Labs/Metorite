# PR #445 — docs: correct four deploy claims the board had wrong, all in the same direction

| | |
|---|---|
| **State** | `merged` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:claude/multi-tenancy-ai-metering-nr8zbj` → `FracktalWorks:main` |
| **Created** | 2026-08-14T20:42:07Z |
| **Merged** | 2026-08-14T20:42:14Z |
| **Closed** | 2026-08-14T20:42:14Z |
| **Head commit** | `dced543396d7fba37b5b9d269f22740bf705e5fa` |
| **Merge commit** | `2f97c87cb7042aee6dc4bcd7151bf22282f422a3` |
| **Archived ref** | `refs/archive/pr/445` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/445 |

## Description

Asked whether anything was left in the session, I opened the deploy logs instead of answering from the merges. **Four claims were false, and every one understated what had shipped.**

## The corrections

| # | Row | Said | Actually |
|---|---|---|---|
| 1 | WS-27 · #439 | "NOT deployed" | Deployed 19:20 UTC, applied a migration |
| 2 | WS-27 · #437 | "NOT deployed" | `171_projects_run_state.sql` in the ledger since `a0af10f` |
| 3 | WS-27 · slice 1 | "NOT merged and NOT deployed" | Both false |
| 4 | WS-31 · #442 | *(silent)* | Deployed; `0 applied` is correct and needed saying |

**Evidence per CLAUDE.md §3.8** — the SHA, the ledger line, the health line, from each run:

```
#439  HEAD is now at aa0d7e30
      175_projects_org_vocabularies.sql ... ok
      Migrations complete (1 applied, 172 already recorded)
      Deploy verified healthy on round 1

#442  HEAD is now at 4934eb8c
      Migrations complete (0 applied, 172 already recorded)
      Deploy verified healthy on round 1
```

`0 applied` on #442 is **correct, not a failed deploy** — `infra/platform/` is the Control Plane's own ladder and the deploy does not apply it, so `platform_api` is on the box and **inert**. Left unstated, a reader seeing "0 applied" could reasonably conclude the deploy did nothing.

## Why this one is worth reading

I wrote "NOT deployed" from the merge without opening the run — the precise habit §3.8 exists to stop. And I spent the same session telling the owner to go and check the deploys.

**A stale negative is worse than silence.** The next slice plans its expand/contract around whether a migration is live, and "NOT deployed" is the kind of claim that gets believed without being rechecked. Migration 171 sat on the board as un-applied for a day while it was live on the box.

## One transient, recorded on purpose

The #439 deploy logged `Job for caddy.service failed.` followed by `Caddy is active` and a healthy verify — a reload blip that self-recovered. Recorded because a silent one would look identical to a real Caddy failure.

## Verification

`uv run pytest tests/unit/test_migration_prefixes.py -q` → 4 passed. Docs-only; `pr-check` skips on `paths-ignore`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01TCncTJFRG1LJqQ5zt395hS

---
_Generated by [Claude Code](https://claude.ai/code/session_01TCncTJFRG1LJqQ5zt395hS)_
