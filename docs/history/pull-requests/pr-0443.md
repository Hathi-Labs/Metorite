# PR #443 — docs(WS-27bj): correct three migration-number references the renumber left behind

| | |
|---|---|
| **State** | `closed` |
| **Author** | @vjvarada |
| **Branch** | `FracktalWorks:claude/projects-app-development-01lepg` → `FracktalWorks:main` |
| **Created** | 2026-08-14T19:28:00Z |
| **Merged** | — |
| **Closed** | 2026-08-14T21:23:07Z |
| **Head commit** | `584c59c9a2187e8f9ad699c55c77e1b9153ce5c2` |
| **Merge commit** | `—` |
| **Archived ref** | `refs/archive/pr/443` |
| **Original** | https://github.com/FracktalWorks/CommandCenter/pull/443 |

## Description

Follow-up to #439, which merged while this was in flight. **Docs and one comment only — no SQL, no behaviour change.**

The migration itself landed on `main` correctly as `175_projects_org_vocabularies.sql`. What did not come out consistent is the prose around it: two sibling sessions renumbered concurrently after the R1 collision, and between them one reference was renumbered that shouldn't have been and one that should have been wasn't.

## The three

**1. `routes/tasks/people.py:642` — renumbered when it shouldn't have been.**

The comment reads "the vocabularies migration … deliberately did NOT put in a CHECK (D-PC-8/P-6)". That is about **People's** vocabularies — the `status` and `employment_type` columns `172_people_profile.sql` adds without a CHECK over live data. It has nothing to do with the Projects vocabularies. Verified rather than assumed: `175_projects_org_vocabularies.sql` does not mention `gtd_people` once, and 172's own header carries the D-PC-8 reasoning. Back to **172**.

**2. `project_management_app.md` §9.11.1 — not renumbered when it should have been.**

It named `172_projects_org_vocabularies.sql`, a file that exists on no branch. A spec pointing at a missing file is the mirror-goes-stale-then-lies failure `CLAUDE.md` §5 names, and this is the first reference anyone reaches for when picking the ticket up. Now **175**.

**3. `work_plan.md` — the collision itself was never recorded.**

Worth a line on the board, because it is the fourth migration-number collision in two weeks and the first one caught by R1's re-check *at merge* rather than in production:

> `main` moved on mid-flight and brought WS-28's `172_people_profile.sql`, plus 173 and 174. Both branches had taken 172. The renumber cost **one `git mv` and one header line**, because `test_projects_org_vocabularies.py` finds the migration **by content** (`"org-wide vocabularies"`) rather than by number — the discipline R1 asks for, now demonstrated rather than asserted.

## Verification

```
uv run pytest tests/unit/test_projects_org_vocabularies.py \
              tests/unit/test_projects_vocabulary_scope.py \
              tests/unit/test_people_profile.py -q
220 passed
```

Blocking lint clean. Every `migration 17x` reference in the touched trees was then audited against the file it actually describes — 172 → People profile, 174 → absences, 175 → Projects vocabularies.

## Not in this PR

**No migration**, so R1 does not apply. `175_projects_org_vocabularies.sql` is unchanged and already on `main`.

## Still owed on WS-27bj

The admin surface for managing org-wide vocabularies. Until it exists an org-wide row can be **created but not edited or retired** — `refuse_org_wide_write` answers 409 rather than letting the rename/merge/delete paths 500 on a `CAST('None' AS uuid)`. That is the conservative half of ship-dark, and it is real debt.

## Owner-gated, unchanged

Deploying — migrations **171 through 175** are now on `main` and applied on no box — and flipping `PROJECTS_ORG_VOCABULARIES`, which is default OFF and gates only the affordance that *creates* an org-wide row, never the read union.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01YTS6p4Fo8apZtQnGSeVZ5j

---
_Generated by [Claude Code](https://claude.ai/code/session_01YTS6p4Fo8apZtQnGSeVZ5j)_
